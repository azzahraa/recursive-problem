FUNCTION palindrome(str : STRING[50], beg,end : INTEGER) : BOOLEAN
	BEGIN
	    IF (beg >= end) THEN
	        RETURN TRUE;
	    ELSE_IF (str[beg] = str[end]) THEN
	        RETURN palindrome(str, beg +1, end -1);
	    ELSE
	        RETURN FALSE
	    END_IF
	END
	

